import { RoleWarehousePipe } from './role-warehouse.pipe';

describe('RoleWarehousePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleWarehousePipe();
    expect(pipe).toBeTruthy();
  });
});
